"auto";
a=device.width;
b=device.height;

while(true){
    toast('12313');
    swipe(a/2,b/8*7,a/2,b/8*1,1000);
    // swipe(a/2,b/16*13,a/2,b/16*10,100);
    // swipe(a/2,b/16*10,a/2,b/16*8,50);
    sleep("2000");
}